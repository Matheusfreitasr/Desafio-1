const notasAline = [8, 9, 7];

function calcularMedia(notas) {
    let soma = 0;
    for (let nota of notas) {
        soma += nota;
    }
    return soma / notas.length;
}

const mediaAline = calcularMedia(notasAline);

console.log("A média das notas da Aline é:", mediaAline);
